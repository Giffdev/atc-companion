# Orchestration Log — Mattingly-1

**Timestamp:** 2026-06-23T13:04:05-07:00  
**Phase:** Phase 1 — D-ATIS Staleness Fix (threshold + testability pass)  
**Outcome:** ✅ success, 15/15 ATIS tests pass

## Task

Change `ATIS_STALE_THRESHOLD_MIN` from 60 → 30; add optional `nowMs` parameter to `parseAtisIssuanceTime` for clean testability (Lovell S1).

## Work Done

- Changed `ATIS_STALE_THRESHOLD_MIN` from `60` to `30` in `src/services/datis.ts`.
- Added `nowMs: number = Date.now()` optional parameter to `parseAtisIssuanceTime`. Replaced internal `new Date()` / `now.getTime()` calls with `nowMs`. Default preserves full backward compatibility — all existing call sites unchanged.
- Updated staleness boundary tests: boundary at 30 min, 31-min → stale, 29-min → not-stale (3 test cases adjusted).

## Verification

| Check | Result |
|-------|--------|
| `npm run lint` | Pre-existing UI errors only; 0 new in datis.ts |
| `npm run build` | ✅ clean |
| `npm run test` — ATIS tests | ✅ 15/15 pass |
| Pre-existing failures | 5 (traffic-service: 3, ops-console-autorefresh: 2) — unchanged |
| New regressions | **0** |

## Artifacts

- `src/services/datis.ts` — modified (threshold 60→30, nowMs param)
- `tests/unit/services/datis-service.test.ts` — modified (3 boundary tests updated)
- Inbox decision: `decisions/inbox/mattingly-atis-threshold.md` → merged into `decisions.md`
