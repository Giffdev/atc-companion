# Orchestration Log — Mattingly (Backend Dev)
**Wave:** Phase 2b Wave 1
**DateTime:** 2026-06-23T20:28:55Z
**Status:** ✅ Completed

## Tasks Executed

| Task | Description | Result |
|------|-------------|--------|
| T1g | `dispatchIntent` regulatory case: `searchFars` → `searchAllRegulatory` (eCFR + ATC 7110.65 + AIM) | ✅ |
| T1h | `facility_info` `overlying`/`underlying`/`general` query types: return `NOT_IMPLEMENTED` 501 instead of silently returning adjacent data | ✅ |

## Files Modified
- `src/services/orchestrator.ts` (T1g, T1h)

## Test Results
- Build: ✅ Clean TypeScript compile, all 16 pages generated
- `npx vitest run tests/unit`: 91 passed / 6 pre-existing failures (traffic-service ×3, facility-adjacency ×1, ops-console-autorefresh ×2)
- Zero new failures introduced

## Notes
- T1g fix closes silent data divergence: `/api/query` regulatory path was returning empty results while `/api/regulatory?scope=all` returned correct data.
- T1h now returns an honest 501 for unsupported facility query types; `adjacent` path unchanged; `airports` branch unchanged.
