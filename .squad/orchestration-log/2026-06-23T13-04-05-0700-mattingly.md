# Orchestration Log — Mattingly

**Timestamp:** 2026-06-23T13:04:05-07:00  
**Phase:** Phase 1 — D-ATIS Staleness Fix  
**Outcome:** ✅ success

## Task

Add `parseAtisIssuanceTime` helper; extend `AtisInfo` with `issuedAt / ageMinutes / stale`; wire `route.ts` to expose new fields.

## Work Done

- Added `export const ATIS_STALE_THRESHOLD_MIN = 60` (threshold tuning delegated to Aaron).
- Implemented `parseAtisIssuanceTime(datis: string): string | null` — extracts `HHMMZ` token, constructs absolute ISO UTC timestamp, handles day-rollover (>5 min future → subtract 24h).
- Extended `AtisInfo` interface: `issuedAt: string | null`, `ageMinutes: number | null`, `stale: boolean`.
- Updated `entries.map()` in `getAtis` to populate all three fields.
- Updated `src/app/api/atis/route.ts` to import `parseAtisIssuanceTime` + `ATIS_STALE_THRESHOLD_MIN` and include `issuedAt / ageMinutes / stale` in per-airport API response.

## Verification

- `npm run build`: ✅ clean
- `npm run test`: 0 new failures; 7 pre-existing unrelated

## Artifacts

- `src/services/datis.ts` — modified
- `src/app/api/atis/route.ts` — modified
- Inbox decision: `decisions/inbox/mattingly-atis-issuetime.md` → merged into `decisions.md`
